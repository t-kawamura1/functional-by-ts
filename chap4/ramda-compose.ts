// import R from 'ramda'
// import zip from 'ramda/src/zip'

const students = ['Roser', 'Turing', 'Kleene', 'Church']
const grades = [80, 100, 90, 99]

// const smartestStudent = R.compose(
//   // R.head,
//   // R.pluck<string>(0),
//   R.reverse,
//   R.sortBy(R.prop(1)),
//   R.zip<string, number>
// )